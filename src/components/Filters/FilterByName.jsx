import React from 'react';

const FilterByName = () => {
    return (
        <div>
            <input type="text" placeholder='title search' />
            
        </div>
    );
};

export default FilterByName;